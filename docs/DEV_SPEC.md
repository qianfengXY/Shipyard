# DEV SPEC

当前运行的是一个真实 agent demo。

要求：

- 优先做最小改动
- 不要重构已有实现
- 只完成当前 `docs/TASKS.md` 中的 demo 任务
- 本次 demo 的 README 标记必须使用全新文本 `Shipyard live demo marker v3.`
- 本次 demo 只需要完成新的演示标记和两个新的演示文档
- 完成后返回结构化结果，不要直接修改 `.shipyard/state.json`
